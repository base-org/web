# `useAvatar`

The `useAvatar` hook is used to get avatar image url from an onchain identity
provider for a given name.

## Usage

```tsx twoslash
import { useAvatar } from '@coinbase/onchainkit/identity';

const { data: avatar, isLoading } = useAvatar({ ensName: 'vitalik.eth' });
```

## Returns

[`useQuery<Promise<GetAddressReturnType>>`](/builderkits/onchainkit/identity/types#getaddressreturntype)

## Parameters

### UseAvatarOptions

[`UseAvatarOptions`](/builderkits/onchainkit/identity/types#useavataroptions)

### UseQueryOptions

[`UseQueryOptions`](/builderkits/onchainkit/identity/types#usequeryoptions)
