# wallet_getPermissions

Defined in [EIP-2255](https://eips.ethereum.org/EIPS/eip-2255)

> Returns the permissions currently granted to the caller.

## Returns

`Array<PermissionObject>`

Returns an array of permission objects. If no permissions have been granted, returns an empty array.

```typescript
interface PermissionObject {
  parentCapability: string; // The name of the method that this permission allows
  invoker: string; // The origin of the requesting site
  caveats?: PermissionCaveat[]; // Optional caveats restricting the permission
}

interface PermissionCaveat {
  type: string; // The name of the caveat
  value: any; // The value of the caveat
}
```

### Example

```json
[
  {
    "parentCapability": "eth_accounts",
    "invoker": "https://example.com",
    "caveats": [
      {
        "type": "restrictReturnedAccounts",
        "value": ["0x0c54fccd2e384b4bb6f2e405bf5cbc15a017aafb"]
      }
    ]
  }
]
```

## Errors

This method does not typically return errors.
